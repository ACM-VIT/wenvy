{
  "name": "@wenvy/web-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "check": "tsc -b --pretty false",
    "dev": "wrangler dev ../../wrangler.jsonc",
    "deploy": "wrangler deploy ../../wrangler.jsonc",
    "cf:types": "wrangler types --config ../../wrangler.jsonc"
  },
  "dependencies": {
    "@wenvy/domain": "workspace:*",
    "hono": "^4.12.25",
    "zod": "^4.2.0"
  }
}
